{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://calculatetokens.com/api/v1/prices.schema.json",
  "title": "Calculate Tokens \u2014 Pricing Data",
  "description": "Schema for public/api/v1/prices.json. Single source of truth for model IDs, tokenizer mapping, pricing, and feature flags.",
  "type": "object",
  "required": [
    "$schema",
    "version",
    "generated_at",
    "last_checked",
    "models"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of the prices.json schema in use"
    },
    "generated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of when this file was last generated"
    },
    "last_checked": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp of the last automated provider pricing-page check. Written by the pricing-check CI job and by nothing else. Document-level rather than per-model because the check runs once per provider page, not per model -- the previous per-model field was written with an identical value for every model, giving 20 lines of daily churn and a guaranteed merge conflict with any open PR editing this file."
    },
    "models": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/definitions/model"
      }
    }
  },
  "definitions": {
    "model": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "display_name",
        "provider",
        "provider_pricing_url",
        "tokenizer",
        "context_window",
        "input_cost_per_1m",
        "output_cost_per_1m",
        "supports_context_caching",
        "context_caching_discount",
        "supports_batch_api",
        "batch_api_discount",
        "thinking_model",
        "thinking_multiplier",
        "thinking_billed_separately",
        "last_human_verified",
        "active"
      ],
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9.]+)*$",
          "description": "Kebab-case unique model identifier"
        },
        "display_name": {
          "type": "string",
          "minLength": 1,
          "description": "Human-readable model name shown in the UI"
        },
        "provider": {
          "type": "string",
          "minLength": 1,
          "description": "Provider company name"
        },
        "provider_pricing_url": {
          "type": "string",
          "pattern": "^https://(openai\\.com|developers\\.openai\\.com|anthropic\\.com|cloud\\.google\\.com|ai\\.google\\.dev|deepseek\\.com|api-docs\\.deepseek\\.com|llama\\.meta\\.com|openrouter\\.ai|mistral\\.ai)/",
          "description": "HTTPS URL to the provider's official pricing page"
        },
        "tokenizer": {
          "type": "string",
          "enum": [
            "cl100k_base",
            "o200k_base",
            "claude",
            "claude-new",
            "gemini",
            "llama",
            "heuristic"
          ],
          "description": "Tokenizer implementation key. 'heuristic' = Math.ceil(chars/4), no Wasm worker spawned."
        },
        "context_window": {
          "type": "integer",
          "minimum": 1,
          "description": "Maximum context window size in tokens"
        },
        "input_cost_per_1m": {
          "type": "number",
          "minimum": 0,
          "description": "USD cost per 1 million input tokens"
        },
        "output_cost_per_1m": {
          "type": "number",
          "minimum": 0,
          "description": "USD cost per 1 million output tokens"
        },
        "supports_context_caching": {
          "type": "boolean",
          "description": "Whether the model supports prompt caching / context caching"
        },
        "context_caching_discount": {
          "oneOf": [
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Fractional discount on cached input tokens (e.g. 0.9 = 90% cheaper)"
            },
            {
              "type": "null"
            }
          ]
        },
        "supports_batch_api": {
          "type": "boolean",
          "description": "Whether the provider offers a batch/async API with reduced pricing"
        },
        "batch_api_discount": {
          "oneOf": [
            {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Fractional discount when using the batch API (e.g. 0.50 = 50% cheaper)"
            },
            {
              "type": "null"
            }
          ]
        },
        "thinking_model": {
          "type": "boolean",
          "description": "Whether this model exposes an extended thinking / chain-of-thought token budget"
        },
        "thinking_multiplier": {
          "oneOf": [
            {
              "type": "number",
              "exclusiveMinimum": 0,
              "description": "Multiplier applied to the thinking token budget when estimating thinking token count"
            },
            {
              "type": "null"
            }
          ]
        },
        "thinking_billed_separately": {
          "type": "boolean",
          "description": "true = thinking tokens add to output cost (OpenAI o-series). false = thinking is bundled in output token price (DeepSeek R1)."
        },
        "requires_js_render": {
          "type": "boolean",
          "description": "true = check-page-changes.js must use Playwright headless rendering instead of fetch (e.g. Anthropic pricing SPA)"
        },
        "last_human_verified": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 timestamp \u2014 updated only when a human has navigated to provider_pricing_url and confirmed values. Drives amber staleness indicators in the UI."
        },
        "active": {
          "type": "boolean",
          "description": "Whether this model is shown in the UI. Set to false to soft-delete without removing from the file."
        },
        "pricing_note": {
          "type": "string",
          "description": "Human-readable note about temporary or introductory pricing, displayed in the UI near cost figures."
        },
        "pricing_note_expires": {
          "type": "string",
          "format": "date",
          "description": "ISO 8601 date (YYYY-MM-DD) after which pricing_note should no longer be displayed."
        },
        "long_context": {
          "type": "object",
          "description": "Optional long-context pricing tier. When the PROMPT exceeds threshold_input_tokens, the entire request -- both input and output -- is billed at these rates instead of the top-level ones. This is a step function on the whole request, not a marginal/graduated rate. Omit for flat-rate models. Providers call this 'long context' (OpenAI, 272K threshold) or tiered prompt pricing (Google Gemini Pro, 200K threshold).",
          "properties": {
            "threshold_input_tokens": {
              "type": "integer",
              "minimum": 1,
              "description": "Prompt token count above which the long-context rates apply. Strictly greater-than: a prompt exactly equal to this value uses standard rates."
            },
            "input_cost_per_1m": {
              "type": "number",
              "minimum": 0,
              "description": "USD cost per 1M input tokens once the threshold is exceeded"
            },
            "output_cost_per_1m": {
              "type": "number",
              "minimum": 0,
              "description": "USD cost per 1M output tokens once the threshold is exceeded"
            }
          },
          "required": [
            "threshold_input_tokens",
            "input_cost_per_1m",
            "output_cost_per_1m"
          ],
          "additionalProperties": false
        },
        "verification_waiver_until": {
          "type": "string",
          "format": "date",
          "description": "ISO 8601 date until which this model is exempt from staleness escalation. For models whose price genuinely cannot be re-verified against a vendor source -- e.g. delisted or vendor-retired models priced from third parties. Waived models are still reported, never silenced; the waiver is a dated commitment capped at 90 days, and an expired one fails CI."
        },
        "verification_waiver_reason": {
          "type": "string",
          "minLength": 10,
          "description": "Why verification is blocked. Required alongside verification_waiver_until."
        }
      },
      "if": {
        "properties": {
          "thinking_model": {
            "const": false
          }
        },
        "required": [
          "thinking_model"
        ]
      },
      "then": {
        "properties": {
          "thinking_multiplier": {
            "type": "null"
          },
          "thinking_billed_separately": {
            "const": false
          }
        }
      },
      "dependentRequired": {
        "verification_waiver_until": [
          "verification_waiver_reason"
        ]
      }
    }
  }
}
